[project]
name = "il-toolcalling-core"
version = "0.1.0"
description = "Shared deterministic tool-calling simulation engine for the Optimus Studio IL tool-calling tasksets."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["il_toolcalling_core"]
